{{yield}}

<div class="jumbotron">
  {{#link-to 'question' question.id}}<span class="question-title">{{question.title}}</span>{{/link-to}}
  <br>
  <span class="author-name">asked by {{question.author}}</span>
  <br>
  <span class="question-content">{{question.content}}</span>
</div>
